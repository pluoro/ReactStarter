import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.containers.Dashboard';

export default defineMessages({
  hello: {
    id: `${scope}.hello`,
    defaultMessage: 'I am dashboard page!',
  },
});
